spec

Features That Must Exist (Combined System)

From the design so far, the firmware must include all of these together:

Core BMS

Multi-bus BMW CSC support

Sequential global module numbering

CSC renumbering system

Mask-based balancing

Pack statistics cache

Deterministic scheduler

State machine safe contactor logic

Diagnostics

Serial console menu

CAN sniffer toggle

Pack summary

Cell voltage table

Temperature table

CSV debug option

CSC discovery / renumber commands

Configuration (from original SimpBMS menu)

Cell over-voltage limit

Cell under-voltage limit

Charge cutoff voltage

Charge hysteresis

Charge max current

Charge end current

Balance start voltage

Balance delta

Battery capacity

Parallel strings

Series cells

Temperature limits

Storage

Persistent settings in flash

SOC saved periodically

Vehicle Integration

Charger control

Precharge logic

IO control

Nextion dashboard


we are using 2 can buses to enable more than 6 csc's to be used, as there are only 6 unique id's available , so far we have implemented, csc's can be on either bus, either bus can have any number of csc's upto the maximum of 6 per bus, there will be duplicated id's across busses but not on the same bus, if duplite id's apprear on the same bus we impliment a scs renumbering comand on that bus, we map moudle by bus and id and the global numbering system is sequential irrelivant of how many modules are on each bus, charger and current sensors can also be on either bus